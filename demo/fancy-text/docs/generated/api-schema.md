# Generated Schema — fancy-text

> This project is a pure frontend SPA — no API schema to generate.

If a backend is added later, regenerate this file from the API definition.

```bash
# Example (if API is added):
# npx openapi-typescript ./openapi.yaml -o src/api/schema.ts
```
